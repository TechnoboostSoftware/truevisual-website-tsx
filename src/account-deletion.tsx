import React from "react";
import ReactDOM from "react-dom/client";
import DeleteAccount from "../components/DeleteAccount";

ReactDOM.createRoot(document.getElementById("account-root")!).render(
  <React.StrictMode>
    <DeleteAccount />
  </React.StrictMode>,
);
